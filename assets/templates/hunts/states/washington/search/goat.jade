.form-group.select-list

  select.form-control( ng-model="hunt.choice.choice_1", ng-options="option.value + ' ' + option.name for option in huntOptions[hunt._id] | orderBy:'value'" )
    option( value="" )

  select.form-control( ng-model="hunt.choice.choice_2", ng-options="option.value + ' ' + option.name for option in huntOptions[hunt._id] | orderBy:'value'" )
    option( value="" )

  select.form-control( ng-model="hunt.choice.choice_3", ng-options="option.value + ' ' + option.name for option in huntOptions[hunt._id] | orderBy:'value'" )
    option( value="" )

  select.form-control( ng-model="hunt.choice.choice_4", ng-options="option.value + ' ' + option.name for option in huntOptions[hunt._id] | orderBy:'value'" )
    option( value="" )
