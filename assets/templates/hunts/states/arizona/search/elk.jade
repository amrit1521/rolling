.form-group.select-list

  select.form-control( ng-model="hunt.choice.huntSelection1", ng-options="option.value as option.name for option in huntOptions[hunt._id] | orderBy:'name'" )
    option( value="" )

  select.form-control( ng-model="hunt.choice.huntSelection2", ng-options="option.value as option.name for option in huntOptions[hunt._id] | orderBy:'name'" )
    option( value="" )

  select.form-control( ng-model="hunt.choice.huntSelection3", ng-options="option.value as option.name for option in huntOptions[hunt._id] | orderBy:'name'" )
    option( value="" )

  select.form-control( ng-model="hunt.choice.huntSelection4", ng-options="option.value as option.name for option in huntOptions[hunt._id] | orderBy:'name'" )
    option( value="" )

  select.form-control( ng-model="hunt.choice.huntSelection5", ng-options="option.value as option.name for option in huntOptions[hunt._id] | orderBy:'name'" )
    option( value="" )
